{
  "name": "escape-room",
  "version": "1.0.0",
  "description": "escape-room",
  "main": "app.js",
  "scripts": {
    "sass:compile": "./node_modules/.bin/sass --no-source-map src/style.scss style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.69.5"
  }
}
